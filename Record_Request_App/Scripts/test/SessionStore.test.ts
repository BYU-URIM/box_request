// import { sessionStore } from "."

// test("Function only pulls departments 1 and 2, which the user has.", () => {
//     expect(
//         sessionStore.userDepartments.map(department => department.id)
//     ).toEqual([1, 2])
// })
